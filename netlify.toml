# Netlify-Deploy-Konfiguration (Vite-SPA)
[build]
  command = "npm run build"
  publish = "dist"

# Single-Page-App: alle Routen auf index.html umleiten
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
